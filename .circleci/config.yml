---
version: 2.1

orbs:
  ecs-deploy: inokappa/ecs-deploy-install@dev:0.0.1

jobs:
  integration:
    docker:
      - image: cimg/base:2020.01
    steps:
      - ecs-deploy/install:
          latest: true

workflows:
  integration_test:
     jobs:
       - integration
